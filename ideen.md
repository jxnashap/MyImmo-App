# Ideen / Backlog

Sammlung von Ideen für später — nicht dringend, bewusst zurückgestellt.

## Charts: nur Portfolio-Entwicklung auf dem Dashboard
- Graphen sollen **nur** auf der Portfolio-Entwicklungsseite (Dashboard) erscheinen —
  **ein** Graph, der die Portfolio-/Immobilienentwicklung darstellt (wie vor PR #9).
- Die mit **PR #9** ergänzten Verlaufs-Charts auf den Seiten **Einnahmen** und **Kosten**
  wieder entfernen (dort nur Tabelle).
- Offen: ob der Zeitraum-Filter (1M / 1J / 5J / Max) auf dem Dashboard-Graphen bleibt
  oder zurück zum festen 12-Monats-Verlauf (alter `CashflowChart`) wie vor PR #9.
- Umsetzung später ggf. per `git revert` des PR-#9-Commits + Anpassung.
